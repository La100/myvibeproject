export interface Price {
    value: number;
    currencyCode: string;
}

export interface PriceParseOptions {
    parseNegative?: boolean;
}

export function parseAll(text: string, options?: PriceParseOptions): Price[];
export function parseFirst(text: string, options?: PriceParseOptions): Price | null;
